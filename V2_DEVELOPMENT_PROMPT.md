# 개인용 주식 터미널 V2 전체 개발 프롬프트

## 역할과 목표

당신은 한국·미국 주식 데이터, Python/Streamlit, 시계열 검증을 다루는 개발자다.
`wlgns2417/buriburi-stock`의 최신 `test.py`를 기준으로 기존 퀀트 투자전략실을
개인용 주식 터미널 V2로 확장하라. 설명이나 예시 화면만 제시하지 말고, 실제 실행
코드·검증·사용 안내까지 완성하여 GitHub에 반영하라.

사용자가 합의한 방향은 다음과 같다.

- 시장 → 섹터 → 종목 → 추세·수급·뉴스 → 관심 판단 → 기록 이후 성과의 흐름.
- 핵심 확장: 급등 전조 탐지기, 섹터 순환 관찰, 외국인·기관 수급 추적.
- 보조 확장: 시장 상황판, 뉴스·공시, 보유 종목 관리, 백테스트와 신호 성과 기록.
- 종목 점수의 절대값뿐 아니라 이전 거래일·5거래일 전 대비 변화도 보여준다.
- 모든 해석은 부드럽고 전문적인 한국어로 쓰고, 데이터 근거와 대응 조건을 명시한다.

## 기존 구조와 유지할 사항

1. 실제 실행 진입점은 `test.py` 하나다. 별도의 Python 모듈 없이 실행 가능해야 한다.
   문서·테스트·패키지 목록은 저장소에 별도로 둘 수 있다.
2. 기존 국장/미장 선택, 종목 분석, 기술 점수, 가격 시나리오, 달리는 말,
   과대낙폭, 백테스트, 차트, 수급 CSV 입력을 유지한다.
3. 현재 다크 테마를 유지하되 상단 V2 메뉴로 각 기능에 접근하도록 한다.
4. 실행에 필요한 라이브러리는 `requirements.txt`에 기록한다.
   예약 화면은 `st.fragment`를 사용하므로 Streamlit 1.37 이상을 명시한다.
5. GitHub에 보유 종목·신호 기록·API 키·비밀번호·연락 이메일을 커밋하지 않는다.
6. 기존 `ho.py`, 개발 컨테이너 설정 등 이번 작업과 무관한 파일은 보존한다.

## 메뉴

`📊 종목 분석 | 🧭 시장 상황판 | 🚨 급등 전조 | 🔥 섹터 순환 |
🐋 수급 추적 | 📰 뉴스·공시 | 💼 내 종목 | 🧪 백테스트·성과`

기본 진입 화면은 기존 종목 분석이다. 레이더와 섹터 결과에서 종목 분석으로
이동할 때 시장·종목 선택을 함께 전달한다. 한국과 미국의 개인 상태·스캔 결과를
분리하고, 시장을 바꾸어도 다른 시장의 결과가 잘못 표시되지 않게 한다.

## 1. 급등 전조 탐지기와 레이더

### 입력·출력

- 한국: 6자리 코드. 미국: 티커 및 클래스 주식 표기.
- 직접 입력 표본은 1~100종목까지 검증한다.
- 한국 전체 스캔은 KOSPI/KOSDAQ 시장 목록과 최소 시가총액 조건을 사용한다.
- 미국 전체 스캔은 실제 확보한 NASDAQ/NYSE 목록을 사용한다.
- 전체 목록 확보에 실패하면 작은 기본 표본을 전체 시장처럼 표시하지 않는다.
- 조회 결과: 전조 점수, 전일 점수 변화, 별도의 기술 점수, 거래량 배수,
  기준일, 관심/관찰 판정, 해석, 배점 근거, 공급원 및 실패 사유.

### 전조 점수 100점: `precursor-2.0`

| 영역 | 최대 배점 | 규칙 |
|---|---:|---|
| 거래량 | 25 | 직전 20일 평균 대비 상승일 2배 이상 25점, 상승일 1.5배 이상 17점, 1.2배 이상 8점 |
| 이동평균 구조 | 25 | 5>20>60 정배열 15점 또는 종가>20일선 7점, 20일선이 5거래일 전보다 상승하면 10점 추가 |
| 모멘텀 | 20 | RSI 50~68은 10점, 40~50 또는 68~75는 5점, MACD 히스토그램 개선 10점 추가 |
| 변동성 수축·돌파 | 15 | 전일 밴드폭이 직전 60일 분포 하위 25%면 7점, 직전 60일 고점 돌파 8점 또는 고점까지 5% 이내 4점 |
| 가격 이격 | 15 | 20일선 대비 0~5%는 15점, -3~0% 또는 5~10%는 8점 |

- RSI>78 또는 20일선 이격>15%이면 과열 감점 10점. 총점은 0~100 범위.
- 75점 이상이며 과열 감점이 없으면 `관심 조건 충족`, 55점 이상이면 `변화 관찰`.
- 거래량 평균과 고점 기준에 현재 봉을 넣지 않는다.
- 최소 71개 확정 일봉으로 현재·전일·5일 전의 동일 모델 점수를 계산한다.
- 투자자별 수급을 모르는 미국 종목에 기관 매집 점수를 주지 않는다.
  수급은 별도 추적 화면에서 확인한다.
- 점수는 급등 확률·승률이 아니며, 예시의 87점 같은 숫자를 고정 입력하지 않는다.

### 스캔 운영

- 한 번에 10종목, 최대 4개 동시 요청. 전체 스캔의 진행률·성공·실패를 기록한다.
- 큰 전체 목록을 스캔할 때 모든 종목의 일봉 DataFrame을 세션에 영구 누적하지 않는다.
- 중간 결과를 확인할 수 있고 다음 배치를 수동으로 이어서 진행할 수 있어야 한다.
- 활성 화면 자동 진행: 1분마다 다음 배치를 처리한다.
- 선택 예약: 거래소 현지 09:30 / 11:00 / 14:00 / 한국 15:30·미국 16:00.
- 예약은 평일 해당 시각부터 5분 이내에만 실행하고, 이미 처리한 예약은 중복 실행하지 않는다.
  지난 예약을 한꺼번에 소급 실행하지 않는다. 실행 중인 전체 스캔과 겹치지 않는다.
- 현재 단계는 확정 일봉 기반이다. 같은 봉이면 장중에 다시 실행해도 같은 점수다.
- 브라우저/해당 화면이 닫히면 계속 실행되는 서버 스케줄러로 설명하지 않는다.

## 2. 시장 상황판

- KOSPI, KOSDAQ, NASDAQ 지수의 확정 종가·일간 수익률·기준일을 조회한다.
- 온도는 다섯 조건(종가>20일선, 종가>60일선, 20일선 상승,
  5거래일 수익률 양수, 20거래일 수익률 양수)을 각 20점으로 표시한다.
- 조회 실패나 오래된 지수에는 가상의 값을 넣지 않는다.
- 최근 스캔 표본의 상승 종목 수와 섹터 강도를 함께 보여준다.
- 시장 전체 투자자 순매수 금액은 검증된 데이터가 없으면 미제공으로 표시한다.
  종목 순매수 수량을 합쳐 시장 전체 현금 유입처럼 표현하지 않는다.

## 3. 섹터 순환

- 한국: 반도체·전력기기·바이오·방산·2차전지 기본 표본.
- 미국: 반도체·대형 기술주·헬스케어·에너지·보안 기본 표본.
- 정의된 구성 종목을 펼쳐볼 수 있게 한다. 수동 테마 표본임을 표시한다.
- 같은 기준일의 최소 2종목이 있어야 해당 섹터 강도를 산출한다.
- 강도는 구성 종목의 전조 점수 평균, 변화는 동일 종목의 5일 점수 변화 평균이다.
- 거래량 배수는 표본 중앙값으로 표시한다.
- 결과를 `실제 자금 유입액` 또는 `공식 업종 전 종목 순위`라고 부르지 않는다.
- 섹터 선택 → 구성 종목 해석 → 기존 종목 분석으로 이어지도록 한다.

## 4. 외국인·기관 수급 추적

- 분석 일봉과 수급 거래일을 정확히 맞춘다.
- 최근 5거래일 방향·거래량 대비 합산 순매수·지속성을 독립 점수로 계산한다.
- 최근 20거래일 자료가 모두 있을 때 연속 순매수와 기관 매수 전환을 표시한다.
- 전 기간 20일 모두 순매수이면 `20일 이상`으로 표시한다.
- 0주 순매매는 매도가 아니며, 결측일을 건너뛰어 연속 일수를 늘리지 않는다.
- 금액은 일별 순매수 수량×해당일 종가의 추정값임을 표시한다.
- 수집 실패를 강한 매도나 매집 부재로 오해시키지 않는다.
- 기존 검증된 수급 CSV 대체 입력을 제공한다.
- 미국 SEC 보유 공시는 일별 순매수와 다르므로 한국식 수급을 추정해 만들지 않는다.

## 5. 뉴스·공시

- 뉴스는 한국어/미국 영문 RSS, 출처 링크와 날짜를 표시한다.
- 제목의 긍정 표현·주의 표현·혼재·판단 보류 단서를 분류한다.
- 제목만 분류한 결과를 원문을 읽은 생성형 AI 분석이나 가격 영향 예측이라고 부르지 않는다.
- 한국 공시는 OpenDART 회사 고유번호와 공시 목록 API를 사용한다.
  최근 90일 공시 제목·날짜·DART 원문 링크를 표시한다.
- 미국 공시는 SEC ticker/CIK 매핑과 submissions API를 사용한다.
  최근 공시 유형·날짜·SEC 원문 링크를 표시한다.
- 한국 공시 자동 조회의 `DART_API_KEY`, SEC의 `SEC_USER_AGENT`는
  Streamlit Secrets 또는 환경변수에서만 읽는다.
- 설정이 없으면 해당 이유와 공식 사이트 링크를 표시한다.
- 인증키가 포함된 요청 URL이나 원래 예외 문자열을 화면·로그에 내보내지 않는다.

## 6. 내 종목

- 시장·코드·이름·보유 수량·평단·참고 손절·목표가를 입력·수정·삭제한다.
- 확정 종가로 평가액·손익·수익률·기준가 도달 상태를 표시한다.
- 통화별로 분리한다. 환율 자료 없이 KRW와 USD 평가액을 합치지 않는다.
- 미조회 종목은 0원으로 대체하지 않는다. 평가 가능한 종목 수를 명시한다.
- 비중은 같은 통화의 평가 가능한 종목 합계에 대해서만 계산한다.
- 개인 정보는 Streamlit 세션에만 보관하고 JSON 내보내기·복원을 제공한다.
- 복원은 시장·종목·수량·가격·중복·신호 모델·날짜를 검증한 뒤 병합한다.
  중복 신호는 현재 기록을 보존해 나중 결과로 이전 판단을 덮어쓰지 않는다.

## 7. 백테스트와 관찰 신호 성과

- 기존 전략 백테스트와 관찰 신호의 이후 성과는 별도 탭이다.
- 레이더의 75점 이상·과열 없음 조건을 충족하면 기록한다.
  사용자가 다른 점수의 관심 신호를 직접 기록할 수도 있다.
- 기록: 모델 버전, 시장, 코드, 신호 일봉 날짜, 실제 기록 시각과 시간대,
  당시 점수·종가·코멘트. 같은 모델/시장/종목/기준일은 한 번만 기록한다.
- 기록일 다음 거래일 이후의 첫 거래 가능한 시가를 가상 진입가로 사용한다.
  오래된 신호를 오늘 기록해도 기록 전 상승분을 성과에 넣지 않는다.
- N=5/10/20거래일의 N번째 종가로 평가하고 양방향 비용을 반영한다.
- 거래량 0인 날 진입하지 않는다. N거래일 미경과·미조회는 평가 대기다.
- 최근 30일 기록 수, 평가 완료 수, 평균 수익률, 양의 수익 비율을 표시한다.
- 완료 표본만 평균·비율에 포함하고 표본 개수를 함께 표시한다.
- 동일 종목·중복 기간 신호의 비독립성, 선택편향, 배당/환율 미반영을 설명한다.
- JSON 복원 이력은 검증된 불변 감사 기록이나 실제 매매 내역으로 취급하지 않는다.

## 코멘트 작성 기준

근거 → 현재 상태 → 확인할 조건 → 대응 방향 순서로 자연스럽게 쓴다.
예시는 조건이 실제로 충족될 때만 출력한다.

> 거래량 증가와 이동평균선 정배열이 함께 관측되고 있습니다. 현재 20일선과의
> 이격이 과도하지 않아 관심 종목으로 살펴보실 만합니다. 조정 시 지지가 유지되는지,
> 가격 회복에 거래가 동반되는지 확인한 뒤 접근을 고려하시는 편이 적절합니다.

외국인·기관 수급이 실제로 확인되지 않으면 위 문장에 동반 순매수·매집·쏠림을
추가하지 않는다. 기술 점수, 전조 점수, 수급 점수, 시장 주목도는 배점과 목적이
다르므로 서로 같은 점수라고 설명하지 않는다.

## 검증·완료 조건

- 문법 검사 및 오프라인 계산 테스트.
- 현재 봉을 제외한 거래량 평균·고점 기준 검증.
- 점수 배점 합계, 과열 감점, 거래량 0, 데이터 부족 처리 검증.
- 미래 정보 혼입 없이 기록 이후 진입·N거래일 평가·비용 처리 검증.
- 날짜가 다른 섹터 표본 제외, 최소 표본수 확인.
- 결측 수급·0주·연속 순매수 처리 검증.
- JSON 비정상 수치·잘못된 코드·중복·모델 불일치 검증.
- 국장/미장 메뉴 이동, 기존 분석 회귀, 스캔 진행, 신호 기록,
  보유 종목 입력·평가·삭제 등 Streamlit AppTest 검증.
- API 실패와 설정 없음에도 화면 전체가 중단되지 않아야 한다.
- 실제 데이터 확인과 모의 데이터 테스트를 구분해 보고한다.
- 변경 파일을 하나의 Git 커밋으로 반영하고, 다른 사용자의 최신 변경을 덮어쓰지 않는다.

## 현재 V2와 추가 연결이 필요한 단계

현재 V2는 **확정 일봉 기반의 개인용 터미널**이다. 아래 사항은 외부 공급원·운영
환경의 추가 결정이 필요하며 완료된 것처럼 표시하지 않는다.

1. 장중 분봉·체결 실시간 레이더: 이용 가능한 증권사 API와 인증·호출 한도 필요.
2. 브라우저 종료 후 24시간 예약: 별도 스케줄러와 개인별 인증된 영구 저장소 필요.
3. 원문 기반 생성형 AI 뉴스·공시 영향 분석: 모델/API 설정과 원문 접근 정책 필요.
4. 전 시장 공식 섹터 구성과 투자자 순매수 금액: 검증된 공급원 필요.
5. 서버 영구 보유 종목 저장: 공개 앱의 사용자 격리·로그인·DB 설정 필요.

위 항목 때문에 가능한 V2 기능 전체를 중단하지 말고, 현재 지원 범위를 화면과
README에 명확히 표시한 실행본을 완성하라.

## 참고 문서

- FinanceDataReader: https://github.com/FinanceData/FinanceDataReader
- Streamlit fragment: https://docs.streamlit.io/develop/api-reference/execution-flow/st.fragment
- OpenDART 공시 검색: https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS001&apiId=2019001
- SEC EDGAR API: https://www.sec.gov/search-filings/edgar-application-programming-interfaces

자동 조건 신호와 직접 기록 신호는 성과 화면의 평가 대상에서 구분합니다. 기본 집계는 자동 조건 신호입니다.
